import SelectPoolDialog from '../../../../components/swap/SelectPoolDialog';

export default function SelectSwapPoolFromList({ onClose }) {
  return <SelectPoolDialog currentPool={'test'} onClose={onClose} />;
}
